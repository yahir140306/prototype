---
// src/pages/cuarto/[id].astro
import { createClient } from "../../lib/supabase";

// Obtener el ID del cuarto desde los parámetros
const { id } = Astro.params;

// Crear cliente de Supabase correctamente
const supabase = createClient({
  request: Astro.request,
  cookies: Astro.cookies,
});

// Obtener datos del cuarto específico
const { data: cuarto, error } = await supabase
  .from("cuartos")
  .select("*")
  .eq("id", id)
  .single();

if (error) {
  console.error("Error obteniendo cuarto:", error);
}
---

<!-- ---
// src/pages/cuarto/[id].astro
import { createClient } from "../../lib/supabase";

// Obtener el ID del cuarto desde los parámetros
const { id } = Astro.params;

// Crear cliente de Supabase correctamente
const supabase = createClient({
  request: Astro.request,
  cookies: Astro.cookies,
});

// Obtener datos del cuarto específico
const { data: cuarto, error } = await supabase
  .from("cuartos")
  .select("*")
  .eq("id", id)
  .single();

if (error) {
  console.error("Error obteniendo cuarto:", error);
}
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{cuarto ? cuarto.name : "Cuarto no encontrado"}</title>
  </head>
  <body>
    <div class="max-w-4xl mx-auto p-4">
      {
        cuarto ? (
          <div>
            <h1 class="text-3xl font-bold mb-4">{cuarto.name}</h1>
            {cuarto.imagen_1 && (
              <img
                src={cuarto.imagen_1}
                alt={cuarto.name}
                class="w-full max-w-md h-64 object-cover rounded-lg mb-4"
              />
            )}
            <p class="text-gray-600 mb-4">{cuarto.descripcion}</p>
            <p class="text-2xl font-bold">${cuarto.precio}/mes</p>
          </div>
        ) : (
          <div>
            <h1 class="text-3xl font-bold mb-4">Cuarto no encontrado</h1>
            <p>El cuarto que buscas no existe.</p>
          </div>
        )
      }
    </div>
  </body>
</html> -->
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{cuarto ? cuarto.name : "Cuarto no encontrado"}</title>
  </head>
  <body>
    <div class="max-w-4xl mx-auto p-4">
      {
        cuarto ? (
          <div>
            <h1 class="text-3xl font-bold mb-4">{cuarto.name}</h1>
            {cuarto.imagen_1 && (
              <img
                src={cuarto.imagen_1}
                alt={cuarto.name}
                class="w-full max-w-md h-64 object-cover rounded-lg mb-4"
              />
            )}
            <p class="text-gray-600 mb-4">{cuarto.descripcion}</p>
            <p class="text-2xl font-bold">${cuarto.precio}/mes</p>
          </div>
        ) : (
          <div>
            <h1 class="text-3xl font-bold mb-4">Cuarto no encontrado</h1>
            <p>El cuarto que buscas no existe.</p>
          </div>
        )
      }
      <button class="p-2 m-2 bg-blue-600"><a href="/">Salir</a></button>
      <button class="p-2 m-2 bg-blue-600"><a href="#">Contactar</a></button>
    </div>
  </body>
</html>
